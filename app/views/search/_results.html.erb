<% if @query.blank? %>
  <p class="text-sm text-zinc-500">Type a name to search across projects, media, clients, and talks.</p>
<% else %>
  <% total = @projects.size + @clients.size + @media_appearances.size + @talks.size %>
  <p class="text-sm tabular-nums text-zinc-500"><%= pluralize(total, "result") %> for “<%= h(@query) %>”</p>

  <% if total.zero? %>
    <div class="rounded-2xl border border-dashed border-zinc-200 bg-zinc-50/80 px-6 py-16 text-center">
      <p class="text-zinc-600">No matches for “<%= h(@query) %>”.</p>
      <p class="mt-2 text-sm text-zinc-500">Try a different name.</p>
    </div>
  <% else %>
    <% if @projects.any? %>
      <section class="flex flex-col gap-3">
        <h2 class="text-sm font-semibold uppercase tracking-wide text-zinc-400">Projects</h2>
        <ul class="flex flex-col gap-2">
          <% @projects.each do |project| %>
            <li>
              <%= link_to project_path_for(project),
                    data: { turbo_frame: "_top" },
                    class: "group flex items-baseline justify-between gap-4 rounded-xl border border-zinc-200/80 bg-white px-5 py-3 shadow-sm shadow-zinc-950/5 transition hover:border-zinc-300 hover:shadow-md hover:shadow-zinc-950/10" do %>
                <span class="font-medium text-zinc-900 group-hover:text-zinc-700"><%= project.name.presence || "Untitled project" %></span>
                <% if project.year.present? %>
                  <span class="shrink-0 text-xs tabular-nums text-zinc-400"><%= project.year %></span>
                <% end %>
              <% end %>
            </li>
          <% end %>
        </ul>
      </section>
    <% end %>

    <% if @media_appearances.any? %>
      <section class="flex flex-col gap-3">
        <h2 class="text-sm font-semibold uppercase tracking-wide text-zinc-400">Media</h2>
        <ul class="flex flex-col gap-2">
          <% @media_appearances.each do |appearance| %>
            <li>
              <%= link_to media_appearance_path_for(appearance),
                    data: { turbo_frame: "_top" },
                    class: "group flex items-baseline justify-between gap-4 rounded-xl border border-zinc-200/80 bg-white px-5 py-3 shadow-sm shadow-zinc-950/5 transition hover:border-zinc-300 hover:shadow-md hover:shadow-zinc-950/10" do %>
                <span class="font-medium text-zinc-900 group-hover:text-zinc-700"><%= appearance.name.presence || "Untitled appearance" %></span>
                <% if appearance.appearance_year.present? %>
                  <span class="shrink-0 text-xs tabular-nums text-zinc-400"><%= appearance.appearance_year %></span>
                <% end %>
              <% end %>
            </li>
          <% end %>
        </ul>
      </section>
    <% end %>

    <% if @clients.any? %>
      <section class="flex flex-col gap-3">
        <h2 class="text-sm font-semibold uppercase tracking-wide text-zinc-400">Clients</h2>
        <ul class="flex flex-col gap-2">
          <% @clients.each do |client| %>
            <li>
              <%= link_to client_path(client),
                    data: { turbo_frame: "_top" },
                    class: "group block rounded-xl border border-zinc-200/80 bg-white px-5 py-3 shadow-sm shadow-zinc-950/5 transition hover:border-zinc-300 hover:shadow-md hover:shadow-zinc-950/10" do %>
                <span class="font-medium text-zinc-900 group-hover:text-zinc-700"><%= client.name %></span>
              <% end %>
            </li>
          <% end %>
        </ul>
      </section>
    <% end %>

    <% if @talks.any? %>
      <section class="flex flex-col gap-3">
        <h2 class="text-sm font-semibold uppercase tracking-wide text-zinc-400">Talks</h2>
        <ul class="flex flex-col gap-2">
          <% @talks.each do |talk| %>
            <li>
              <% talk_url = talk_path_for(talk) %>
              <% if talk_url %>
                <%= link_to talk_url,
                      data: { turbo_frame: "_top" },
                      **talk_link_options(talk),
                      class: "group flex items-baseline justify-between gap-4 rounded-xl border border-zinc-200/80 bg-white px-5 py-3 shadow-sm shadow-zinc-950/5 transition hover:border-zinc-300 hover:shadow-md hover:shadow-zinc-950/10" do %>
                  <span class="font-medium text-zinc-900 group-hover:text-zinc-700"><%= talk.name.presence || "Untitled talk" %></span>
                  <% if talk.talk_year.present? %>
                    <span class="shrink-0 text-xs tabular-nums text-zinc-400"><%= talk.talk_year %></span>
                  <% end %>
                <% end %>
              <% else %>
                <div class="flex items-baseline justify-between gap-4 rounded-xl border border-zinc-200/80 bg-zinc-50 px-5 py-3">
                  <span class="font-medium text-zinc-700"><%= talk.name.presence || "Untitled talk" %></span>
                  <% if talk.talk_year.present? %>
                    <span class="shrink-0 text-xs tabular-nums text-zinc-400"><%= talk.talk_year %></span>
                  <% end %>
                </div>
              <% end %>
            </li>
          <% end %>
        </ul>
      </section>
    <% end %>
  <% end %>
<% end %>

<% content_for :title, @media_appearance.name.presence || "Media appearance" %>

<div class="mx-auto flex w-full max-w-4xl flex-1 flex-col gap-8 px-5 pb-16 pt-10 sm:px-8">
  <nav class="text-sm" aria-label="Breadcrumb">
    <%= link_to "Media", media_path, class: "font-medium text-zinc-500 transition hover:text-zinc-800" %>
    <span class="mx-2 text-zinc-300" aria-hidden="true">/</span>
    <span class="text-zinc-900" aria-current="page"><%= @media_appearance.name.presence || "Untitled" %></span>
  </nav>

  <article class="overflow-hidden rounded-2xl border border-zinc-200/80 bg-white shadow-sm shadow-zinc-950/5">
    <div class="p-6 sm:p-8">
      <div class="flex flex-wrap items-start justify-between gap-4">
        <div class="flex flex-wrap items-center gap-2">
          <% if @media_appearance.appearance_date.present? %>
            <span class="inline-flex rounded-md bg-zinc-100 px-2.5 py-1 text-xs font-medium text-zinc-600">
              <%= l(@media_appearance.appearance_date, format: :long) %>
            </span>
          <% end %>
          <% if @media_appearance.archived? %>
            <span class="inline-flex rounded-md bg-zinc-200 px-2.5 py-1 text-xs font-medium text-zinc-700">Archived</span>
          <% end %>
        </div>
        <% if @media_appearance.notion_url.present? %>
          <%= link_to @media_appearance.notion_url, target: "_blank", rel: "noopener noreferrer", class: "inline-flex items-center gap-1.5 rounded-lg border border-zinc-200 bg-white px-3 py-1.5 text-sm font-medium text-zinc-700 shadow-sm transition hover:border-zinc-300 hover:bg-zinc-50" do %>
            Open in Notion
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="h-4 w-4 text-zinc-400" aria-hidden="true">
              <path fill-rule="evenodd" d="M4.25 5.5a.75.75 0 00-.75.75v8.5c0 .414.336.75.75.75h8.5a.75.75 0 00.75-.75v-4a.75.75 0 011.5 0v4A2.25 2.25 0 0112.75 17h-8.5A2.25 2.25 0 012 14.75v-8.5A2.25 2.25 0 014.25 4h5a.75.75 0 010 1.5h-5z" clip-rule="evenodd" />
              <path fill-rule="evenodd" d="M6.194 12.753a.75.75 0 001.06.053L16.5 4.44v2.81a.75.75 0 001.5 0v-4.5a.75.75 0 00-.75-.75h-4.5a.75.75 0 000 1.5h2.553l-9.056 9.194a.75.75 0 00-.053 1.06z" clip-rule="evenodd" />
            </svg>
          <% end %>
        <% end %>
      </div>

      <h1 class="mt-4 text-3xl font-semibold tracking-tight text-zinc-900 sm:text-4xl">
        <%= @media_appearance.name.presence || "Untitled appearance" %>
      </h1>

      <% meta_bits = [@media_appearance.organizer, @media_appearance.location].compact %>
      <% if meta_bits.any? %>
        <p class="mt-3 text-base text-zinc-600"><%= meta_bits.join(" · ") %></p>
      <% end %>

      <% if @media_appearance.topic.present? %>
        <p class="mt-6 text-base leading-relaxed text-zinc-700"><%= @media_appearance.topic %></p>
      <% end %>

      <% if @media_appearance.project.present? %>
        <div class="mt-8 border-t border-zinc-100 pt-8">
          <h2 class="text-xs font-semibold uppercase tracking-wide text-zinc-400">Related project</h2>
          <p class="mt-2">
            <%= link_to @media_appearance.project.name,
                  project_path(@media_appearance.project),
                  class: "text-sm font-medium text-zinc-700 transition hover:text-zinc-900" %>
          </p>
        </div>
      <% end %>

      <% if @media_appearance.notion_created_at.present? || @media_appearance.notion_last_edited_at.present? || @media_appearance.last_synced_at.present? %>
        <footer class="mt-8 border-t border-zinc-100 pt-6 text-xs text-zinc-400">
          <ul class="flex flex-col gap-1 sm:flex-row sm:flex-wrap sm:gap-x-6 sm:gap-y-1">
            <% if @media_appearance.notion_created_at.present? %>
              <li>Created in Notion <%= l(@media_appearance.notion_created_at, format: :long) %></li>
            <% end %>
            <% if @media_appearance.notion_last_edited_at.present? %>
              <li>Last edited <%= l(@media_appearance.notion_last_edited_at, format: :long) %></li>
            <% end %>
            <% if @media_appearance.last_synced_at.present? %>
              <li>Synced <%= time_ago_in_words(@media_appearance.last_synced_at) %> ago</li>
            <% end %>
          </ul>
        </footer>
      <% end %>
    </div>
  </article>
</div>
